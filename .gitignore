# Build artefacts
target/
build/

# Local SQLite database (created by `mvn spring-boot:run`)
msc.db

# Local payload fixtures used by the smoke test / curl examples
payload*.json

# IDE / OS noise
.idea/
*.iml
*.ipr
*.iws
.vscode/
.DS_Store

# Maven Wrapper jar (regenerated by `mvn wrapper:wrapper`)
.mvn/wrapper/maven-wrapper.jar