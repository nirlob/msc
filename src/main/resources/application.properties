# MSC Reference Server — runtime config
spring.application.name=msc-reference-server
server.port=8000

# SQLite location (use ":memory:" in tests)
msc.db.path=./msc.db

# Logging
logging.level.io.msc=INFO
logging.level.org.springframework.web=INFO